# In dev, leave VITE_API_URL unset — Vite proxies /api and /pdfs to localhost:5001.
# In production (Cloudflare Pages), set this to your deployed backend's API URL:
# VITE_API_URL=https://<your-backend>.onrender.com/api
VITE_API_URL=
